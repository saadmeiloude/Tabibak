# إضافة خاصية الدفع الإلكتروني (المحفظة) لواجهة الطبيب
**التاريخ:** 2025-12-21
**الحالة:** ✅ تم الحل

## المشكلة
خاصية الدفع الإلكتروني (المحفظة) لا تظهر في واجهة الطبيب.

## الحل المطبق

### 1. إضافة المحفظة إلى القائمة السفلية
**الملف:** `lib/screens/doctor/doctor_layout.dart`

#### التغييرات:
1. **استيراد شاشة المحفظة:**
   ```dart
   import '../wallet_screen.dart'; // Import Wallet Screen
   ```

2. **إضافة المحفظة إلى قائمة الشاشات:**
   ```dart
   final List<Widget> _screens = [
     const DoctorHomeScreen(),
     const AppointmentManagementScreen(),
     const PatientListScreen(),
     const WalletScreen(), // ← جديد
     const DoctorSettingsScreen(),
   ];
   ```

3. **إضافة عنصر المحفظة في القائمة السفلية:**
   ```dart
   BottomNavigationBarItem(
     icon: Icon(Icons.account_balance_wallet),
     label: AppLocalizations.of(context)?.wallet ?? 'المحفظة',
   ),
   ```

### 2. إضافة زر الوصول السريع للمحفظة
**الملف:** `lib/screens/doctor/doctor_home_screen.dart`

تم إضافة بطاقة إجراء سريع للمحفظة في الشاشة الرئيسية:
```dart
_buildQuickActionCard(
  loc?.wallet ?? 'المحفظة',
  Icons.account_balance_wallet,
  Colors.purple,
  () => Navigator.pushNamed(context, '/wallet'),
),
```

### 3. إضافة الترجمات
**الملفات:** 
- `lib/l10n/app_ar.json`
- `lib/l10n/app_fr.json`

تم إضافة مفتاح `wallet` للترجمة:
- **العربية:** "المحفظة"
- **الفرنسية:** "Portefeuille"

### 4. توليد ملفات الترجمة
تم تشغيل الأمر:
```bash
flutter gen-l10n
```

## النتيجة

✅ **القائمة السفلية:** تحتوي الآن على 5 عناصر:
1. الرئيسية (Home)
2. التقويم (Calendar)
3. المرضى (Patients)
4. **المحفظة (Wallet)** ← جديد
5. ملفي (Profile)

✅ **الإجراءات السريعة:** تحتوي الآن على 4 بطاقات:
1. موعد جديد
2. مريض جديد
3. التقارير
4. **المحفظة** ← جديد

## كيفية الوصول إلى المحفظة

### الطريقة الأولى: القائمة السفلية
1. افتح تطبيق الطبيب
2. اضغط على أيقونة المحفظة 💳 في القائمة السفلية

### الطريقة الثانية: الإجراءات السريعة
1. افتح الشاشة الرئيسية للطبيب
2. في قسم "الإجراءات السريعة"
3. اضغط على بطاقة "المحفظة" البنفسجية

## ميزات المحفظة المتاحة

✅ عرض الرصيد الحالي
✅ عرض سجل المعاملات
✅ الإيداع (Deposit)
✅ السحب (Withdrawal)
✅ إحصائيات المحفظة

## ملاحظات
- المحفظة تعمل لكل من المرضى والأطباء
- الأطباء يمكنهم استلام المدفوعات من المرضى
- يمكن للأطباء سحب أرباحهم من المحفظة
