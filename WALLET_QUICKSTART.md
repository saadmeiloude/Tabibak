# 🚀 دليل البدء السريع - نظام المحفظة

## ⚡ خطوات التفعيل (5 دقائق)

### الخطوة 1️⃣: تطبيق قاعدة البيانات

**افتح phpMyAdmin:**
1. اذهب إلى http://localhost/phpmyadmin
2. اختر قاعدة البيانات `tabibak`
3. اضغط على تبويب "SQL"
4. افتح الملف: `backend/database/quick_wallet_setup.sql`
5. انسخ كل المحتوى والصقه
6. اضغط "تنفيذ" (Go)

✅ **النتيجة المتوقعة:** رسالة "تم إنشاء جداول نظام المحفظة بنجاح!"

---

### الخطوة 2️⃣: اختبار النظام

**في التطبيق:**
1. افتح التطبيق (يجب أن يكون يعمل بالفعل)
2. سجل الدخول أو أنشئ حساب جديد
3. اذهب إلى **الملف الشخصي** (آخر أيقونة في الشريط السفلي)
4. اضغط على **"المحفظة والرصيد"**

✅ **النتيجة المتوقعة:** شاشة المحفظة مع رصيد 0.00 أوقية

---

### الخطوة 3️⃣: تجربة الشحن

1. في شاشة المحفظة، اضغط **"شحن"**
2. أدخل مبلغ تجريبي: `500`
3. اختر طريقة الدفع: **بطاقة ائتمان**
4. اضغط **"شحن"**

✅ **النتيجة المتوقعة:** 
- رسالة نجاح
- الرصيد الجديد: 490.00 أوقية (بعد خصم رسوم 2%)
- ظهور المعاملة في تبويب "المعاملات"

---

### الخطوة 4️⃣: تجربة السحب

1. اضغط **"سحب"**
2. أدخل مبلغ: `100`
3. اختر: **تحويل بنكي**
4. أدخل:
   - اسم البنك: `البنك الوطني`
   - رقم الحساب: `123456789`
   - اسم صاحب الحساب: `اسمك`
5. اضغط **"سحب"**

✅ **النتيجة المتوقعة:**
- رسالة: "تم إنشاء طلب السحب بنجاح"
- خصم 105 أوقية (100 + 5 رسوم)
- الرصيد الجديد: 385.00 أوقية
- حالة الطلب: "قيد الانتظار"

---

## 🎯 اختبار سريع للـ API

### اختبار 1: الحصول على الرصيد
```bash
# في المتصفح أو Postman
http://localhost:8000/api/wallet/get_balance.php?user_id=1
```

### اختبار 2: إيداع رصيد
```bash
# في Postman - POST Request
URL: http://localhost:8000/api/wallet/deposit.php
Body (JSON):
{
  "user_id": 1,
  "amount": 1000,
  "payment_method": "card"
}
```

### اختبار 3: عرض المعاملات
```bash
# في المتصفح
http://localhost:8000/api/wallet/get_transactions.php?user_id=1&limit=10
```

---

## 📊 التحقق من قاعدة البيانات

**في phpMyAdmin، نفذ:**

```sql
-- عرض جميع المحافظ
SELECT * FROM wallets;

-- عرض جميع المعاملات
SELECT * FROM transactions ORDER BY created_at DESC LIMIT 10;

-- عرض طلبات السحب
SELECT * FROM withdrawal_requests;

-- عرض إحصائيات مستخدم
SELECT 
    u.full_name,
    w.balance,
    COUNT(t.id) as total_transactions
FROM users u
LEFT JOIN wallets w ON u.id = w.user_id
LEFT JOIN transactions t ON w.id = t.wallet_id
WHERE u.id = 1
GROUP BY u.id;
```

---

## ❓ حل المشاكل الشائعة

### 🔴 المشكلة: "المحفظة غير موجودة"
**الحل:**
```sql
-- أنشئ محفظة يدوياً
INSERT INTO wallets (user_id, balance, currency) VALUES (1, 0.00, 'MRU');
```

### 🔴 المشكلة: خطأ في الاتصال بالـ API
**الحل:**
1. تأكد من تشغيل السيرفر: `php -S localhost:8000`
2. تحقق من المسار في `wallet_service.dart`:
   ```dart
   static const String baseUrl = 'http://localhost:8000/api/wallet';
   ```

### 🔴 المشكلة: لا تظهر المعاملات
**الحل:**
```sql
-- تحقق من وجود معاملات
SELECT * FROM transactions WHERE user_id = 1;
```

---

## 🎉 تهانينا!

إذا نجحت جميع الخطوات، فأنت الآن لديك:
- ✅ نظام محفظة كامل
- ✅ إيداع وسحب الأموال
- ✅ سجل معاملات مفصل
- ✅ إحصائيات مالية
- ✅ واجهة مستخدم جميلة

---

## 📚 للمزيد من المعلومات

اقرأ الملفات التالية:
- `WALLET_README.md` - دليل شامل
- `WALLET_SETUP.md` - تفاصيل التثبيت
- `backend/database/schema_wallet.sql` - هيكل قاعدة البيانات الكامل

---

**وقت التنفيذ المتوقع:** 5-10 دقائق  
**المستوى:** مبتدئ  
**الدعم:** متوفر عبر GitHub Issues
